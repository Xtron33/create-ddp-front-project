echo "🏗️  Билдим проект перед пушем..."

# Запускаем сборку
yarn build

# Проверяем результат сборки
if [ $? -ne 0 ]; then
    echo "❌ Упали, давай фиксить."
    exit 1
fi

echo "✅ Все зашибок! Пушим..."
