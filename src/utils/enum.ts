export enum Technology {
    React  = "React",
    Next = "Next"
}

export enum Router {
    ReactRouter = "ReactRouter",
    TanStackRouter = "TanStackRouter",
    Wouter = "Wouter"
}

export enum StateManager {
    Without = "Without",
    Zustand = "Zustand",
    RTK = "RTK",
    Effector = "Effector"
}
